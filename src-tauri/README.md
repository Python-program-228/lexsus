# src-tauri/ — Rust Core

Tauri backend written in Rust.

**Status: placeholder.** This directory is empty and will be populated when the Rust core is scaffolded.

## Planned contents
- `git2` — diffs, status, staging, branch, commit history, commit from the app
- `portable-pty` — observe Claude Code and power the interactive terminal pane
- OS-native file watchers (fsevents / inotify / ReadDirectoryChangesW)
- SQLite storage for Session Archive + Structured Project Memory
- Local IPC for the browser extension (web-AI tool-call relay)

_See `docs/tech-stack.md` for the full stack._
